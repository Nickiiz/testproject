import 'package:flutter/material.dart';

class CloudMate extends StatelessWidget {
  const CloudMate({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: FlutterLogo(
        size: 300,
      ),
    );
  }
}
